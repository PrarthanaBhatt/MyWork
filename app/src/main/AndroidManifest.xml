<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.prarthana.myworkapplication">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyWorkApplication">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name=".location_map.MapsActivity"
            android:label="@string/title_activity_maps"></activity>

        <activity android:name=".location_map.LocationMainActivity" />
        <activity android:name=".location_map.MapMainActivity" />
        <activity android:name=".data_binding.BindMainActivity" />
        <activity android:name=".data_binding.DataBindingMainActivity" />
        <activity android:name=".movie_db.MovieDetailsActivity" />
        <activity android:name=".movie_db.MovieMainActivity" />
        <activity android:name=".room_orm.RoomOrmMainActivity" />
        <activity android:name=".data_binding_recycler.DataBindingRecyclerMainActivity" />
        <activity android:name=".api_calls.HttpUrlConnection_call.HttpUrlConnectionMainActivity" />
        <activity android:name=".api_calls.okhttp_call.OkHttpMainActivity" />
        <activity android:name=".architecture.mvc_architecture.MvcMainActivity" />
        <activity android:name=".architecture.mvp_architecture.MvpMainActivity" />
        <activity android:name=".architecture.mvvm_architecture.MvvmMainActivity" />
        <activity android:name=".architecture.ArchitectureMainActivity" />
        <activity android:name=".api_calls.volley_call.VolleyMainActivity" />
        <activity android:name=".api_calls.retrofit_call.RetrofitMainActivity" />
        <activity android:name=".data_Activity.DataMainActivity" />
        <activity android:name=".api_calls.ApiCallMainActivity" />
        <activity android:name=".data_persistence.internal_storage.InternalStorageMainActivity" />
        <activity android:name=".data_persistence.sql_data.FetchActivity" />
        <activity android:name=".data_persistence.sql_data.SqlMainActivity" />
        <activity android:name=".data_persistence.shared_pref.SharedPrefActivity" />
        <activity android:name=".components.service_one.CallServiceActivity" />
        <activity android:name=".components.contentprovider_one.ChooseImageActivity" />
        <activity android:name=".components.notification.NotificationActivity" />
        <activity android:name=".layout.layout_dashboard.DashboardLayoutActivity" />
        <activity android:name=".layout.MyConstraint.MyConstraintLayoutActivity" />
        <activity android:name=".layout.MyRelative.MyRelativeLayoutActivity" />
        <activity android:name=".layout.MyLinear.MyLinearLayoutActivity" />
        <activity android:name=".data_persistence.DataPersistenceMainActivity" />
        <activity android:name=".activity_lifecycle.LifecycleMainActivity" />
        <activity android:name=".components.ComponentsMainActivity" />
        <activity android:name=".layout.LayoutMainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <receiver android:name=".components.notification.NotificationReceiver" />

        <service android:name=".components.service_one.MyService2" />
    </application>
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

</manifest>